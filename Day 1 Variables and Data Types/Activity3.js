// **Task 4:** Create variables of different data types (number, string, boolean, object, array) and log each variable's type using the `typeof` operator.

let num = 69
let str = "hello"
let bol = true
let intro = {
    name: "Harshit",
    rollno: 118
}
let arr = [1, 2, 3, 4, 5, 6]

console.log(typeof(num)) // This is number
console.log(typeof(str)) // This is string
console.log(typeof(bol)) // This is boolean
console.log(typeof(intro)) // This is of type object
console.log(typeof(arr))  // Arrays is also a type object 